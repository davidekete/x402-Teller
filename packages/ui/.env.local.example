NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here

# Backend URL where the facilitator is running (Express/Hono app)
# This is used to fetch the facilitator's public keys for authentication (server-side)
BACKEND_URL=http://localhost:3002

# Client-side accessible backend URL (for fetching data from browser)
NEXT_PUBLIC_BACKEND_URL=http://localhost:3002
